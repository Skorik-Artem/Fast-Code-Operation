#include <iostream>
#include <conio.h>
#include <string>

using namespace std;

int main()
{

    string frase;


    cout << "Input frase: " << endl;
    getline(cin, frase);

    for (int i = 0; i < frase.length(); i++)
        frase[i] = tolower(frase[i]);


    cout << frase;


}
